node_modules
dist
*.local
.DS_Store

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*

# Your actual habit data stays local. Recreate with `pnpm db:migrate && pnpm db:seed`.
data/*.db
data/*.db-shm
data/*.db-wal

# Generated by vue-router's file-based routing plugin
typed-router.d.ts

# Subagent-driven development scratch
.superpowers/

# Local secrets — never commit. Phase 1 adds this too; it lives here so main is safe on its own.
.env
